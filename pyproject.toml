# Tooling config for the repo. Not a Python package (no [build-system]/[project]).

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
# pycodestyle, pyflakes, isort, warnings, pyupgrade, bugbear.
select = ["E", "F", "I", "W", "UP", "B"]
