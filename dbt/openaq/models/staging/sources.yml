version: 2

sources:
  - name: bronze
    database: OPENAQ
    schema: "{{ var('source_schema', 'BRONZE') }}"
    tables:
      - name: measurements
        description: Raw OpenAQ measurements published by bronze ingestion.
      - name: location_snapshots
        description: Raw OpenAQ location discovery snapshots published by bronze ingestion.
      - name: load_summary
        description: Completed Bronze refresh windows used to synchronize incremental measurement models.
