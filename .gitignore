# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Environment / secrets
.env
.env.*
!.env.example

# Astro / Airflow
# Commit .astro/config.yaml and the parse helper so `astro dev start` works on a
# fresh clone; ignore only the local/volatile bits (Astro's own convention).
.astro/standalone/
.astro/*.local.yml
.astro/*.local.yaml
airflow_settings.yaml
webserver_config.py
airflow-webserver.pid
logs/
airflow.db
airflow.cfg

# dbt
target/
dbt_packages/
**/target/
**/dbt_packages/
**/logs/

# Editors / local tooling
.idea/
.vscode/
.claude/settings.local.json
